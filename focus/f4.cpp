/* 
* File : f4.cpp
* Project : SENG1000
* Programmer: Jaskaran Kainth
* First version : 2022-01-02
* Description : A program to find the lowest integer based on an users input.
*/ 

#include <iostream>

using namespace std;

// numbers = input, minimum = smallest integer from the array
// numIndex = elements position in the input of the array.

using namespace std;

int main(){
int array[31] = {};
int minTemp, maxTemp, i;
int arrSize = sizeof(array)/sizeof(array[0]);
int rangechecker(minTemp, maxTemp);

cout << "enter 31 numbers as numbers \n";
cin >> array[31];
cout << "Enter a min number:" << minTemp; 
cout << "Enter a max number:" << maxTemp;

int getTemperatrues(array){
    while (array == -99){
        return 0;
    }
    cout << "you have entered " << arrSize << "numbers \n";
    }

int SearchTemperatures(array, arrSize, maxTemp, minTemp){

    for(array i = minTemp; i<= maxTemp; i++){
        return array;
    }
    }

}





